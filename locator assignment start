spaceLocator -p 0 0 0;

//take selected objects, find the average position and create a locator at that position.
//create a locator at the center of each selected object
//boundingBox (xform Flag) used in a midpoint formula 

/*
	in a for loop 
	get translation attributes of each selected object
	create a locator at the center of each selected object
	
	find the center of all of the selected objects
	create a locator at that center location
*/


{
	string $selection[] = `ls -sl`;
	float $posX[]; 
	float $posY[]; 
	float $posZ[];
	float $midpointX;
	float $midpointY;
	float $midpointZ;
	
	for( $i=0; $i<size($selection); $i++)
	{
	    
	    string $selectionI = $selection[$i];
		float $X = `getAttr ($selectionI + ".translateX")`;
		float $Y = `getAttr ($selectionI + ".translateY")`;
		float $Z = `getAttr ($selectionI + ".translateZ")`;
				
		spaceLocator -absolute -position $X $Y $Z;
				
	}
	
	for($i=0; $i<size($selection); $i++)
	{
	    string $selectionI = $selection[$i];
		$posX[$i] = `getAttr ($selectionI + ".translateX")`; 
		$posY[$i] = `getAttr ($selectionI + ".translateY")`; 
		$posZ[$i] = `getAttr ($selectionI + ".translateZ")`; 
		
		
	}

    $midpointX = (

}